<div class="comment">
  <strong><%= image_tag(comment.author.avatar_url(:thumb)) %></strong>
  <em>on <%= comment.created_at.strftime('%b %d, %Y at %I:%M %p') %></em>
  <%= simple_format comment.content %>
</div>